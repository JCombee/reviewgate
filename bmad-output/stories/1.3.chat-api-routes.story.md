<!--
  STORY CONTEXT OBJECT — bmad-epics-and-stories
  LOCKED SECTIONS: Acceptance Criteria, Dev Notes, and Testing are LOCKED.
  External dev tools MUST NOT edit them.
-->

# 1.3: Chat-id-aware API routes + SSE `chatId` tagging

**Story ID:** 1.3
**Epic:** 1 — Multiple Chats
**Slug:** chat-api-routes
**Status:** done

## Story

As a **web client**,
I want **HTTP routes and SSE events that identify which chat a message or token belongs to**,
so that **the browser can route streamed answers and history to the right chat thread instead of assuming there is only one**.

## Acceptance Criteria

1. `POST /api/review/:id/chat` (`packages/server/src/app.ts`) is replaced with `POST /api/review/:id/chats/:chatId/messages`, which validates `{ message: string }` exactly as today and calls `Session.chat(chatId, message)`.
2. A new `POST /api/review/:id/chats` route accepts an optional `{ title?, model? }` body, calls `Session.createChat(title, model)`, and returns the updated `Review`.
3. The `chat-token` SSE event (`packages/core/src/api.ts`'s `ReviewEvent` union) gains a required `chatId: string` field; `Session`'s emission of that event (`session.ts`) includes the chat id the token belongs to.
4. `packages/web/src/lib/reviewClient.ts`'s `ReviewApi.chat` becomes `chat: (chatId: string, message: string) => Promise<void>`, and a new `createChat: (title?: string, model?: string | null) => Promise<void>` is added; `subscribeToReview`'s `onChatToken` handler signature becomes `(chatId: string, text: string) => void`.
5. Existing non-chat routes and their tests in `app.ts`/`app.test.ts` are unaffected.

## Tasks / Subtasks

- [ ] Replace the `/chat` route with `/chats/:chatId/messages`, reading `chatId` from the path param (AC: #1)
- [ ] Add the `POST /chats` route for chat creation (AC: #2)
- [ ] Add `chatId` to the `chat-token` member of `ReviewEvent` in `packages/core/src/api.ts` (AC: #3)
- [ ] Update `Session`'s `#emit({ type: "chat-token", ... })` call site to include `chatId` (AC: #3)
- [ ] Update `ReviewApi.chat`/add `ReviewApi.createChat` in `reviewClient.ts`, update the `send`/`run` helper call sites (AC: #4)
- [ ] Update `subscribeToReview`'s `onChatToken` handler type and its dispatch of `event.chatId` (AC: #4)
- [ ] Run the existing `app.test.ts` suite and add a route-level test for the two new/changed chat routes (AC: #1, #2, #5)

## Dev Notes

- Current route: `app.post("/api/review/:id/chat", ...)` validates `body?.message` is a non-empty string, calls `s.chat(message)`, returns `{ review }` on success or `{ error }` with 503 on `AgentUnavailable`-style failure. [Source: packages/server/src/app.ts:237-251]
- `Session.chat` becomes `chat(chatId, message)` per Story 1.2 — this story only needs to thread `c.req.param("chatId")` into that call; no new error-handling shape is needed beyond what already exists (missing/invalid `chatId` surfaces as the same `503 { error }` path via `Session`'s thrown error, AC #4 of Story 1.2). [Source: bmad-output/stories/1.2.server-chat-map.story.md]
- `ReviewEvent` is defined as a discriminated union `{ type: "review"; review } | { type: "chat-token"; text } | { type: "pass"; status } | { type: "ping" }` in `packages/core/src/api.ts:111-115` — add `chatId: string` only to the `chat-token` member, not the others. [Source: packages/core/src/api.ts]
- `subscribeToReview` (`packages/web/src/lib/reviewClient.ts:80-102`) currently does `else if (event.type === "chat-token") handlers.onChatToken?.(event.text)`; this becomes `handlers.onChatToken?.(event.chatId, event.text)`. The `EventSource` listener registration loop (`for (const name of ["review", "chat-token", "pass"])`) is unaffected — event *names* don't change, only the payload shape. [Source: packages/web/src/lib/reviewClient.ts]
- `ReviewApi.chat` is called from exactly one place today, `ChatPanel.tsx`'s `send()` — that call site is Story 1.4's responsibility to update; this story only changes the `ReviewApi` interface and its `reviewClient.ts` implementation, accepting that `ChatPanel.tsx` will not compile again until Story 1.4 lands (same pattern as Story 1.2 → 1.3). [Inference]
- Keep the response shape `{ review: Review }` identical to every other mutating route in `app.ts` (see `mutate()` helper at `app.ts:134` and its call sites) — do not special-case chat routes' response envelope. [Source: packages/server/src/app.ts]

## Testing

- **Approach:** integration tests in `packages/server/src/app.test.ts`, following its existing pattern of exercising routes through the Hono app instance (no real SDK calls — `Session`'s agent calls should already be mockable per Story 1.2's test setup).
- **Key scenarios:** `POST /chats` creates a chat and it appears in the returned `Review.chats`; `POST /chats/:chatId/messages` on a valid chat id appends messages and streams a `chat-token` event carrying that `chatId`; the same route on an unknown `chatId` returns an error response (503, matching the existing `AgentUnavailable` convention) rather than a 500 or silent success.
- **Per-AC verification:** AC #1 → route test posts a message to a created chat and asserts the review reflects it; AC #2 → route test posts to `/chats` and asserts a new `Chat` appears; AC #3 → an SSE-level test (or a unit test on `Session`'s emit call) asserts the emitted event object has `chatId`; AC #4 → `reviewClient.ts` type-checks against the updated `ReviewApi` interface and a unit test (mocking `fetch`/`EventSource`) confirms `chat(chatId, message)` posts to the new path; AC #5 → the full existing `app.test.ts` suite passes unchanged.
- **Fixtures / mocks:** a stub/mock `Session` (or the real `Session` with a stubbed `ReviewAgent`) wired the same way existing `app.test.ts` cases already set up sessions for other routes.
- **Edge cases to cover:** posting a message with `chatId` referring to a chat that was just created in the same test (race-free, since it's sequential); malformed/missing `chatId` path param (Hono routing already guarantees a string here, so this reduces to "unknown id" rather than "wrong type").

## Dependency Maps

- **Blocked by:** 1.1 (needs `Chat`/`ReviewEvent.chatId` shape), 1.2 (needs `Session.chat(chatId, message)`/`createChat`)
- **Blocks:** 1.4 (UI needs the updated `ReviewApi`/SSE contract), 2.2 (model picker's `createChat`/model-change calls go through this same `ReviewApi` surface)
- **External:** none

## Owned File/Module Scope

- `packages/server/src/app.ts`
- `packages/server/src/app.test.ts`
- `packages/core/src/api.ts` (adds `chatId` to the `chat-token` `ReviewEvent` member only)
- `packages/web/src/lib/reviewClient.ts`

_(`packages/core/src/api.ts` was also touched by Story 1.1, which this story is Blocked-by — see Dependency Maps.)_

## Learnings from Previous Stories

- From 1.2: `Session.chat` now takes `(chatId, message)` and `Session.createChat(title?, model?)` exists — this story is purely the HTTP/SSE surface over those two methods, no new session-level logic is needed.
- From 1.1: `ReviewStore.load` already migrates legacy single-chat reviews into `chats: [...]` before `Session` or `app.ts` ever see them, so route handlers never need to special-case the old shape.

## Dev Agent Record

**Implementation summary:**

- `packages/server/src/app.ts`: replaced `POST /api/review/:id/chat` with `POST /api/review/:id/chats/:chatId/messages` (reads `chatId` from the path param, validates `{ message }` exactly as before, calls `Session.chat(chatId, message)`) and added `POST /api/review/:id/chats` (accepts optional `{ title?, model? }`, calls `Session.createChat(title, model)`). Both routes keep the existing `{ review }` success / `{ error }` + 503 catch-all shape used by the old route (AC #1, #2).
- `packages/core/src/api.ts`: `ReviewEvent`'s `chat-token` member gained a required `chatId: string` field; the other union members (`review`, `pass`, `ping`) are untouched (AC #3).
- `packages/server/src/session.ts`: updated the one `#emit({ type: "chat-token", ... })` call site inside `chat()` to include `chatId` (AC #3). **Scope deviation:** `session.ts` is not listed in this story's Owned File/Module Scope, but AC #3 (locked) explicitly requires "`Session`'s emission of that event (`session.ts`) includes the chat id," so this one-line change was made anyway — no other part of `session.ts` was touched.
- `packages/web/src/lib/reviewClient.ts`: `ReviewApi.chat` is now `(chatId, message) => Promise<void>`; added `ReviewApi.createChat(title?, model?)`; `ReviewEventHandlers.onChatToken` is now `(chatId, text) => void`; the SSE dispatch passes `event.chatId` through. The `EventSource` listener-name registration loop was left unchanged per the Dev Notes (event *names* on the wire don't change, only payload shape) (AC #4).
- `packages/server/src/app.test.ts`: added a new `describe("chat", ...)` block (6 tests) covering: chat creation appears in `review.chats`; default title/model when omitted; posting a message appends it to the right chat's `messages` (mocking `ReviewAgent.prototype.ask`); an empty message is rejected with 400; an unknown `chatId` returns 503 with the id in the error message (not a 500 or silent success); and an SSE-level test confirming the `chat-token` event payload carries the correct `chatId`.
- Existing non-chat tests in `app.test.ts` were left untouched and still pass (AC #5).

**Judgment calls / notes for the record:**

- SSE event framing: the server's `/events` stream multiplexes every mutation under the same wire-level SSE frame name, `event: review` (see `app.ts`'s stream loop, pre-existing and unrelated to this story — `queue.shift()` values are always written as `{ event: "review", data }` regardless of the JSON payload's own `type`). Consumers (both the existing client code and my new test) tell events apart by the `type` field inside the JSON payload, not by the SSE frame's `event:` name. My first draft of the SSE test asserted on `event: chat-token` literally appearing in the stream text, which is wrong given this pre-existing behavior; I corrected it to assert on `"type":"chat-token"` in the payload instead, matching how `reviewClient.ts`'s `subscribeToReview` actually dispatches events. This is documented here because it's a subtlety worth knowing for Story 1.4 testing too: the `chat-token` addEventListener registration on the client is effectively dead code (it never fires, since the server never emits a frame literally named `chat-token`), but the feature still works correctly because the `"review"`-named frame's handler dispatches on the JSON `type` field generically. Not fixed here since AC #4 only asked for the payload/type shape, not the wire framing, and fixing it is out of this story's scope.
- Confirmed `packages/cli` has no references to `.chat(`, `review.chat`, or `onChatToken`, so no CLI changes were needed.
- Left `packages/web/src/components/ChatPanel.tsx` and `App.tsx` untouched — they still call the old 1-arg `ReviewApi.chat(message)` shape and `onChatToken: (text) => ...`, which is Story 1.4's explicit responsibility. Note for whoever picks up 1.4: `App.tsx`'s current `onChatToken: (text) => setStreaming((prev) => (prev ?? "") + text)` will type-check against the new 2-arg signature (TypeScript allows a callback with fewer declared params) but is semantically wrong until updated — `text` there would actually receive the `chatId` argument at runtime.

**Verification:**

- `npx tsc -b packages/core packages/server packages/cli` — clean (first point since Story 1.1 where the full non-web build is completely clean).
- `npx tsc --noEmit -p packages/web/tsconfig.json` — only the 5 pre-existing `ChatPanel.tsx` errors remain (Story 1.4's job); `reviewClient.ts` itself compiles clean.
- `npx vitest run packages/core packages/server` — 219/219 passing (includes the 27 tests in `app.test.ts`, up from 21 before this story).
- Full `npm test` (whole-repo, `pretest` → `tsc -b packages/core packages/server packages/cli` → `vitest run`) — 263/263 passing, build clean. This is the first story since 1.1 where the full repo-wide gate passes end to end.
