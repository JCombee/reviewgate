# Keep line endings stable across platforms. The diff parser reads what git hands
# over and normalises nothing itself (§4), so the repo must not look different per
# platform.
* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
